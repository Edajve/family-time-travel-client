export const BackHeaderStyles = (theme) => ({
    backHeaderContainer: {
        width: '100%'
        , height: '70px'
    }
    , backHeader: {
        color: theme.colors.textMain
        , marginTop: '10px'
    }
})

export default BackHeaderStyles